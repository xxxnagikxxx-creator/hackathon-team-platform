# ITAMHack - Платформа для формирования команд на хакатонах

Веб-платформа для поиска сокомандников и формирования команд на хакатонах. Сервис помогает участникам быстро находить людей со схожими навыками и целями, а организаторам — контролировать процесс формирования команд и получать аналитику по аудитории.

## 🚀 Основные возможности

- **Управление хакатонами**: создание, редактирование и просмотр информации о хакатонах
- **Формирование команд**: поиск сокомандников, создание команд, отправка приглашений
- **Профили участников**: детальная информация о навыках и опыте участников
- **Система уведомлений**: уведомления о приглашениях в команды и других событиях
- **Административная панель**: управление хакатонами, экспорт данных в CSV
- **Telegram-бот**: интеграция с Telegram для удобного взаимодействия

## 🛠 Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **PostgreSQL** - реляционная база данных
- **Redis** - кэширование и управление сессиями
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - миграции базы данных
- **Aiogram** - фреймворк для Telegram-бота
- **JWT** - аутентификация и авторизация

### Frontend
- **React 19** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **React Router** - маршрутизация
- **TanStack Query** - управление состоянием сервера
- **SCSS** - препроцессор CSS
- **Axios** - HTTP-клиент

### Инфраструктура
- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация контейнеров
- **Nginx** - веб-сервер для фронтенда (в продакшене)

## 📋 Требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки без Docker)
- Node.js 18+ и npm (для локальной разработки фронтенда)

## 🔧 Установка и запуск

### Быстрый старт с Docker

1. **Клонируйте репозиторий**
   ```bash
   git clone <repo-url> itamhack
   cd itamhack
   ```

2. **Создайте файл `.env`**
   ```bash
   cp env.example .env
   ```

3. **Настройте переменные окружения в `.env`**
   ```env
   # Обязательно измените эти значения!
   DB_USER=your_db_user
   DB_PASSWORD=your_strong_password_here
   DB_NAME=itamhack
   SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token
   ```

4. **Сгенерируйте секретный ключ** (если нужно)
   ```bash
   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

5. **Запустите проект**
   ```bash
   # Для разработки
   docker compose up -d --build
   
   # Для продакшена
   docker compose -f docker-compose.prod.yaml up -d --build
   ```

6. **Создайте администратора**
   ```bash
   docker compose exec backend python -m backend.api.create_admin
   ```

### Локальная разработка (без Docker)

#### Backend

1. **Установите зависимости**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

2. **Настройте базу данных**
   - Установите PostgreSQL и Redis
   - Создайте базу данных
   - Настройте переменные окружения в `.env`

3. **Примените миграции**
   ```bash
   alembic upgrade head
   ```

4. **Запустите сервер**
   ```bash
   uvicorn backend.api.main:app --reload --host 0.0.0.0 --port 8000
   ```

#### Frontend

1. **Установите зависимости**
   ```bash
   cd frontend
   npm install
   ```

2. **Запустите dev-сервер**
   ```bash
   npm run dev
   ```

3. **Соберите для продакшена**
   ```bash
   npm run build
   ```

## 📁 Структура проекта

```
ITAMHack/
├── backend/                 # Backend приложение
│   ├── api/
│   │   ├── admin/          # Административные функции
│   │   ├── bot/            # Telegram-бот
│   │   ├── hackathons/     # Управление хакатонами
│   │   ├── profile/        # Профили пользователей
│   │   ├── teams/          # Управление командами
│   │   ├── redis/          # Redis сервисы
│   │   ├── config.py       # Конфигурация
│   │   ├── database.py     # Настройка БД
│   │   └── main.py         # Точка входа API
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/                # Frontend приложение
│   ├── src/
│   │   ├── app/            # Конфигурация приложения
│   │   ├── entities/       # Бизнес-сущности
│   │   ├── features/       # Функциональные возможности
│   │   ├── pages/          # Страницы приложения
│   │   ├── shared/         # Общие компоненты и утилиты
│   │   └── widgets/        # Виджеты
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yaml      # Docker Compose для разработки
├── docker-compose.prod.yaml # Docker Compose для продакшена
├── env.example              # Пример файла окружения
└── README.md
```

## 🔐 Конфигурация

Основные переменные окружения (см. `env.example`):

- `DB_USER`, `DB_PASSWORD`, `DB_NAME` - настройки PostgreSQL
- `REDIS_HOST`, `REDIS_PORT` - настройки Redis
- `SECRET_KEY` - секретный ключ для JWT (минимум 32 символа)
- `TELEGRAM_BOT_TOKEN` - токен Telegram-бота
- `API_URL` - URL API сервера

## 📚 API Документация

После запуска backend, документация API доступна по адресам:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## 🧪 Разработка

### Основные команды

```bash
# Запуск всех сервисов
docker compose up -d

# Просмотр логов
docker compose logs -f [service_name]

# Остановка
docker compose down

# Пересборка
docker compose build --no-cache
docker compose up -d
```

### Frontend команды

```bash
cd frontend
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для продакшена
npm run lint     # Проверка кода
npm run preview  # Предпросмотр продакшен-сборки
```

## 🚢 Развертывание

Подробные инструкции по развертыванию см. в файлах:
- `DEPLOY_QUICK.md` - быстрая шпаргалка
- `DEPLOY.md` - подробная инструкция

## 📝 Основные функции

### Для участников
- Регистрация и авторизация
- Создание и редактирование профиля
- Просмотр списка хакатонов
- Поиск и присоединение к командам
- Отправка приглашений в команды
- Просмотр уведомлений

### Для администраторов
- Управление хакатонами (создание, редактирование, удаление)
- Просмотр статистики по командам
- Экспорт данных команд в CSV
- Управление пользователями

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан для хакатона ITAMHack.

## 👥 Авторы

Студенты:
Молчанов Роман
Сырцов Игорь
Кирсанов Екатерина

## 🐛 Известные проблемы

Если вы столкнулись с проблемами, проверьте:
1. Логи контейнеров: `docker compose logs`
2. Настройки в `.env` файле
3. Доступность портов
4. Ресурсы системы: `docker stats`

